[package]
name = "forkstify"
version = "0.1.0"
edition = "2021"
description = "Lecteur de musique par branches — PoC : navigation à sec"

[dependencies]
anyhow = "1"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
